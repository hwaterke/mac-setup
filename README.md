# Mac Setup (archived)

This repo is no longer maintained.

My mac setup now lives in my dotfiles repo (`hwaterke/dotfiles`, private),
managed with [chezmoi](https://www.chezmoi.io/). A single
`chezmoi init --apply` bootstraps a new machine: dotfiles, Homebrew and all
packages, oh-my-zsh, and macOS preferences.
